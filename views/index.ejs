<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Index</title>
</head>
<body>
    <h1>Superheroes</h1>
    <ul>
        <% for (let i = 0; i < superheroes.length; i++) { %>
            <li><a href="/superheroes/<%=i%>"><%= superheroes[i].name %></a></li>
        <% } %>
    </ul>
    <br>
    <h1>Villains</h1>
    <ul>
        <% for (let i = 0; i < villains.length; i++) { %>
            <li><a href="/villains/<%=i%>"><%= villains[i].name %></a></li>
        <% } %>
    </ul>
</body>
</html>